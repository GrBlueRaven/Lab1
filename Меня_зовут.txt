#include <string>//библиотека для работ со строками
#include <iostream>//библиотека ввода-вывода
#include <Windows.h>//библиотека Win32
using namespace std;//пользовательское пространство имен в программе
void main() //тип войд, () означают, что функция не возвращает и не принимает значения
{
	SetConsoleCP(1251);//ввод на русском языке
	SetConsoleOutputCP(1251);//вывод на русском языке
	int num; // тип переменной и идентификатор
	cout << "Введите номер курса\n";
	cin >> num;
	string f, i, o;
	cout << "Введите ФИО\n";
	cin >> f >> i >> o;
	int age;
	cout << "Введите возраст\n";
	cin >> age;
	cout << "Меня зовут " << f <<" " << i <<" " << o << "\nЯ учусь на " << num << " курсе"; // \n - перенос строки, 
	cout << endl; // перенос строки
	if (num <= 2 && age>25)
	{
		cout << "Возраст больше 25\n";
	}
	else
	{
		cout << "Возраст меньше 25\n";
	}
}
